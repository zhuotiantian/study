var isMatch = function (s, p) {
  console.log(s == p);
  var reg = /[\.\*]/g;
  if (reg.test(reg)) {

  } else {
    console.log(s == p);
    return s == p
  }
};
` `
console.log(isMatch("aaa", "a"));
